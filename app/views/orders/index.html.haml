%h1 Listing orders

%table
  %thead
    %tr
      %th User
      %th Ordered
  %tbody
    - @orders.each do |order|
      %tr
        %td= order.user.email
        %td= order.created_at.strftime("%T %A %d %B")
        %td= link_to t('actions.show'), order

%hr/

