%h1.text-center.page_headline
  = t('activerecord.models.menu.other')

%div.content
  %ul.nav.nav-tabs
    - @facade.week_menus.each do |menu|
      %li.nav-item
        = link_to menu.name, menu

%br/
- if current_user.lunch_admin?
  = button_to "#{t('actions.new')} #{t('activerecord.models.menu.one')}", new_menu_path, method: :get, class: 'btn btn-primary'

